<% content_for :title, "Admin - Disciplina" %>

<div class="space-y-4 rounded-3xl bg-white p-6 shadow-sm ring-1 ring-slate-200">
  <p class="text-sm font-semibold text-emerald-700">Disciplina</p>
  <h1 class="text-3xl font-black text-slate-900"><%= @subject.name %></h1>
  <p class="text-slate-600"><%= @subject.description %></p>
</div>
