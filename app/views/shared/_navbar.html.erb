<nav class="border-b border-slate-200 bg-white/90 backdrop-blur">
  <div class="mx-auto flex max-w-7xl items-center justify-between gap-4 px-4 py-4 sm:px-6 lg:px-8">
    <div class="flex items-center gap-3">
      <div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-emerald-500 text-lg font-black text-white">M</div>
      <div>
        <%= link_to root_path, class: "block" do %>
          <p class="text-sm font-semibold text-emerald-700">Matemática Sem Mistério</p>
          <p class="text-xs text-slate-500">Aprenda brincando</p>
        <% end %>
      </div>
    </div>

    <div class="flex flex-wrap items-center gap-2">
      <% if user_signed_in? %>
        <%= nav_link_to "Painel", dashboard_path, active_paths: [dashboard_path] %>
        <%= nav_link_to "Lições", lessons_path, active_paths: [lessons_path] %>
        <%= nav_link_to "Ranking", ranking_index_path, active_paths: [ranking_index_path] %>
        <%= nav_link_to "Conquistas", achievements_path, active_paths: [achievements_path] %>
        <%= nav_link_to "Perfil", profile_path, active_paths: [profile_path] %>
        <% if current_user.admin? %>
          <%= nav_link_to "Admin", admin_subjects_path, active_paths: [admin_subjects_path, admin_lessons_path, admin_questions_path, admin_users_path] %>
        <% end %>
        <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "rounded-full bg-slate-900 px-4 py-2 text-sm font-semibold text-white hover:bg-slate-700" %>
      <% else %>
        <%= nav_link_to "Entrar", new_user_session_path %>
        <%= link_to "Criar conta", new_user_registration_path, class: "rounded-full bg-emerald-500 px-4 py-2 text-sm font-semibold text-white hover:bg-emerald-600" %>
      <% end %>
    </div>
  </div>
</nav>
