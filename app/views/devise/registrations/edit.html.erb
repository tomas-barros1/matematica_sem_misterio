<% content_for :title, "Editar conta" %>

<div class="auth-shell reveal">
  <div class="auth-rail">
    <p class="math-chip bg-white/15 text-white">Perfil</p>
    <h1 class="mt-4 text-4xl font-black tracking-tight">Editar conta</h1>
    <p class="mt-3 text-sm leading-6 text-slate-200">Atualize seus dados e mantenha sua jornada.</p>
  </div>

  <div class="p-8">
    <%= render "devise/shared/error_messages", resource: resource %>

    <%= form_with scope: resource_name, url: registration_path(resource_name), method: :put, class: "mt-6 space-y-4" do |form| %>
    <div>
      <%= form.label :name, "Nome", class: "mb-2 block text-sm font-semibold text-slate-700" %>
      <%= form.text_field :name, autofocus: true, class: "w-full rounded-2xl border-slate-300 px-4 py-3" %>
    </div>
    <div>
      <%= form.label :email, "Email", class: "mb-2 block text-sm font-semibold text-slate-700" %>
      <%= form.email_field :email, autocomplete: "email", class: "w-full rounded-2xl border-slate-300 px-4 py-3" %>
    </div>
    <div>
      <%= form.label :password, "Senha", class: "mb-2 block text-sm font-semibold text-slate-700" %>
      <%= form.password_field :password, autocomplete: "new-password", class: "w-full rounded-2xl border-slate-300 px-4 py-3" %>
    </div>
    <div>
      <%= form.label :password_confirmation, "Confirmar senha", class: "mb-2 block text-sm font-semibold text-slate-700" %>
      <%= form.password_field :password_confirmation, autocomplete: "new-password", class: "w-full rounded-2xl border-slate-300 px-4 py-3" %>
    </div>
    <div>
      <%= form.label :current_password, "Senha atual", class: "mb-2 block text-sm font-semibold text-slate-700" %>
      <%= form.password_field :current_password, autocomplete: "current-password", class: "w-full rounded-2xl border-slate-300 px-4 py-3" %>
    </div>
      <%= form.submit "Salvar", class: "w-full rounded-full bg-emerald-500 px-5 py-3 text-sm font-bold text-white hover:bg-emerald-600" %>
    <% end %>
  </div>
</div>
